[project]
name = "certafix-backend"
version = "0.1.0"
description = "CertaFix Handyman Services — REST API"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
